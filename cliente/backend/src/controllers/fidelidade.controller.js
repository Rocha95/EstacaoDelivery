import { prisma } from '../lib/prisma.js'
export async function obter(req,res){const config=await prisma.configuracao.findUnique({where:{id:req.estabelecimentoId==='default'?'default':`config-${req.estabelecimentoId}`}});const fidelidade=await prisma.fidelidade.upsert({where:{usuarioId_estabelecimentoId:{usuarioId:req.usuario.id,estabelecimentoId:req.estabelecimentoId}},update:{},create:{usuarioId:req.usuario.id,estabelecimentoId:req.estabelecimentoId}});res.json({pontosSaldo:fidelidade.pontosSaldo,pontosAcumulados:fidelidade.pontosAcumulados,pontosPorReal:Number(config?.pontosPorReal||1)})}
export async function historico(req,res){const fidelidade=await prisma.fidelidade.findUnique({where:{usuarioId_estabelecimentoId:{usuarioId:req.usuario.id,estabelecimentoId:req.estabelecimentoId}},include:{movimentos:{orderBy:{criadoEm:'desc'},take:50}}});res.json(fidelidade?.movimentos||[])}
