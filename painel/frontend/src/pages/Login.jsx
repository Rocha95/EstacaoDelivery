import { useState } from 'react'
import { useNavigate, useLocation } from 'react-router-dom'
export default function Login(){const [email,setEmail]=useState('');const [senha,setSenha]=useState('');const [erro,setErro]=useState('');const [loading,setLoading]=useState(false);const nav=useNavigate();const loc=useLocation();const entrar=async(e)=>{e.preventDefault();setErro('');setLoading(true);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({email,senha})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.erro||d.message||'Não foi possível entrar.');localStorage.setItem('painelUsuario',JSON.stringify(d.usuario));nav(loc.state?.from?.pathname||'/',{replace:true})}catch(e){setErro(e.message)}finally{setLoading(false)}};return <div className="login-page"><form className="login-card" onSubmit={entrar}><div className="login-brand"><strong>ESTAÇÃO</strong><span>DELIVERY</span></div><h1>Entrar no painel</h1><p>Gerencie pedidos, cardápio e operação do estabelecimento.</p>{erro&&<div className="login-error">{erro}</div>}<div className="field"><label>E-mail</label><input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required /></div><div className="field"><label>Senha</label><input type="password" autoComplete="current-password" value={senha} onChange={e=>setSenha(e.target.value)} required /></div><div className="login-session">🔒 Sua sessão permanecerá ativa por até 24 horas neste dispositivo.</div><button className="btn btn-primary login-button" disabled={loading}>{loading?'Entrando...':'Entrar'}</button></form></div>}
